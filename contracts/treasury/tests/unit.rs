/*
use shade_protocol::c_std::{
    coins, from_binary, to_binary,
    DepsMut, Addr, StdError,
    Binary, StdResult, Response, Env,
    Response, Uint128,
};

#[test]
fn test_function(param0, param1) {
    assert_eq!(param0, param1);
}

macro_rules! test_function_tests {
    ($($name:ident: $value:expr,)*) => {
        $(
            #[test]
            fn $name() {
                let (param0, param1) = $value;
                test_function(param0, param1);
            }
        )*
    }
}

test_function_tests! {
    test_function_0: (
        Uint128::new(1),
        Uint128::new(1),
    ),
    test_function_1: (
        Uint128::new(1),
        Uint128::new(2),
    ),
}
*/
